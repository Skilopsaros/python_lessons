import random

my_names = ["Ermis", "Giorgos", "Vasiliki", "Inigo Montoya", "Ece"]
print(random.choice(my_names)) # chooses and returns a random value from a list

print(random.randint(1, 9)) # returns a random integer between the first and second value, inclusive

print(random.random()) # returns a random float between 0 and 1.0


